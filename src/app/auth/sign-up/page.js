'use client'
import * as React from 'react';


export default function SignUp(){
    const [data,setData] = React.useState({//provide emails password});});

        const handleObject = (e) =>{
        setData(item =>({
            const {name,value} = e.target
            ...item,[name]:value
        }))

        }
    }
    return(
        <div>

        </div>
    )
}